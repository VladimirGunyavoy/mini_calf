# Выученные уроки - Stage 2

**Формат записи**:
```
## Lesson N: [Дата YYYY-MM-DD] Краткое название

**Проблема**: Что случилось
**Решение**: Как исправили
**Вывод**: Что запомнить на будущее
```

---

# Уроки из предыдущих фаз (Phase 0-9)

Перенесено из `../PROMPT_FOR_NEXT_AGENT.md` для reference.

---

## Lesson 1: [2025-12-17] Кодировка Windows

**Проблема**: `UnicodeEncodeError` при использовании эмодзи и кириллицы в `print()` на Windows.

**Решение**: Используй только ASCII символы в print() или английский текст.

**Вывод**: На Windows избегай Unicode в консольном выводе. Используй английский для print().

---

## Lesson 2: [2025-12-17] Фазовое пространство для визуализации

**Проблема**: Нужно визуализировать динамику системы [x, v].

**Решение**: Используй фазовое пространство в визуализации:
```python
# Phase space representation: (x, y, v)
x = state[0]  # Position
v = state[1]  # Velocity
position = (x, 0, v)  # X-axis = position, Z-axis = velocity
```

**Вывод**: Фазовое пространство XZ (позиция × скорость) - отличный способ визуализации траекторий 1D систем.

---

## Lesson 3: [2025-12-17] PointSystem внешнее управление

**Проблема**: VectorizedEnvironment устанавливает `env.u = action`, но PointSystem не использует `self.u`.

**Решение**: Исправлен `compute_derivative()`:
```python
# Если контроллера нет, используем self.u (для внешнего управления)
if self.controller is not None:
    u = self.controller.get_control(state)
else:
    u = self.u if self.u is not None else 0.0
```

**Вывод**: `self.u` должен быть скаляром, не массивом. Конвертируй через `float(u.item())` если нужно.

---

## Lesson 4: [2025-12-17] Преобразование numpy array к скаляру

**Проблема**: `ValueError: setting an array element with a sequence` когда `u` - массив.

**Решение**:
```python
# Преобразуем u в скаляр, если это массив
if isinstance(u, np.ndarray):
    u = float(u.item()) if u.size == 1 else float(u[0])
else:
    u = float(u)
```

**Вывод**: Всегда проверяй тип `u` и конвертируй numpy array → float если нужен скаляр.

---

## Lesson 5: [2025-12-17] Масштабируемость визуализации

**Проблема**: Производительность при большом количестве агентов.

**Результаты тестов**:
- 10 агентов: ~3500 FPS ⚡
- 50 агентов: ~1260 FPS ⚡
- 100 агентов: ~580 FPS ✅
- 200 агентов: ~300 FPS ✅

**Вывод**: VectorizedEnvironment отлично масштабируется. Batch обработка через `get_actions_batch()` эффективна.

---

## Lesson 6: [2025-12-17] Адаптивный scale для множества агентов

**Проблема**: При большом количестве агентов они визуально сливаются.

**Решение**: Уменьшай scale визуальных объектов с ростом количества агентов:
```python
scale = max(0.04, 0.08 - (n_agents / 2000))  # Меньше для большего количества
```

**Вывод**: Динамический scale улучшает читаемость визуализации при множестве агентов.

---

## Lesson 7: [2025-12-17] PowerShell vs Bash синтаксис

**Проблема**: `&&` не работает в PowerShell.

**Решение**:
```bash
# ❌ НЕ РАБОТАЕТ в PowerShell:
cd path && python script.py

# ✅ РАБОТАЕТ:
cd path; python script.py
```

**Вывод**: В PowerShell используй `;` вместо `&&` для последовательных команд.

---

## Lesson 8: [2025-12-17] Организация тестов

**Проблема**: Тесты разбросаны по проекту.

**Решение**: Все тесты в папке `tests/` с добавлением родительской директории в sys.path:
```python
import sys
from pathlib import Path
sys.path.insert(0, str(Path(__file__).parent.parent))
```

**Вывод**: Централизованная папка tests/ с правильным sys.path упрощает организацию.

---

## Lesson 9: [2025-12-17] Тестирование визуализации

**Проблема**: Визуализация может запуститься без ошибок, но работать неправильно.

**Решение**: При запуске тестов с визуализацией:
1. Запусти тест
2. ДОЖДИСЬ подтверждения от пользователя, что визуализация работает корректно
3. НЕ продолжай работу, пока пользователь не скажет "работает"

**Вывод**: Всегда жди подтверждения пользователя после визуальных тестов.

---

## Lesson 10: [2025-12-17] Глобальная update() функция в Ursina

**Проблема**: Функция `update()` внутри `main()` не вызывается Ursina.

**Решение**: Сделать `update()` глобальной функцией:
```python
# ❌ НЕ РАБОТАЕТ:
def main():
    app = Ursina()
    def update():  # Локальная функция - Ursina не видит!
        pass
    app.run()

# ✅ РАБОТАЕТ:
vec_env = None  # Global variables
def update():  # Глобальная функция
    global vec_env
    # update code
def main():
    global vec_env
    app = Ursina()
    app.run()
```

**Вывод**: `update()` и `input()` в Ursina должны быть глобальными функциями.

---

## Lesson 11: [2025-12-17] Оптимизация траекторий

**Проблема**: Низкий FPS при отрисовке траекторий - mesh перестраивается каждый кадр.

**Решение**: Добавить параметры оптимизации:
```python
trail = SimpleTrail(
    max_length=600,          # Максимум точек
    decimation=5,            # Добавлять каждую 5-ю точку
    rebuild_frequency=20     # Перестраивать mesh каждые 20 добавлений
)
```

**Результат**:
- 10 агентов: `decimation=2`, `rebuild_frequency=10`
- 50 агентов: `decimation=5`, `rebuild_frequency=20`
- Значительное улучшение FPS

**Вывод**: Decimation и rebuild_frequency критичны для производительности траекторий.

---

## Lesson 12: [2025-12-17] Цветовая схема визуализации

**Проблема**: Ярко-белый фон и объекты могут быть ослепительными. `color.rgb()` работает непредсказуемо.

**Решение**: Использовать Vec4 с диапазоном 0-1:
```python
# ✅ Темный фон
Sky(color=Vec4(0.04, 0.04, 0.08, 1))  # Темно-синее небо

# ✅ Темный пол БЕЗ текстуры
ground = Entity(
    model='plane',
    color=Vec4(0.12, 0.12, 0.16, 1),  # Темно-серый
    # НЕ добавляй texture='white_cube' - это сделает пол белым!
)
```

**Цветовые палитры для CALF режимов (Vec4)**:
- TD3: `Vec4(0.2, 0.3, 0.8, 1)` - синий
- Relax: `Vec4(0.2, 0.6, 0.3, 1)` - зеленый
- Fallback: `Vec4(0.8, 0.4, 0.15, 1)` - оранжевый

**Вывод**: Используй Vec4(R/255, G/255, B/255, 1) для надёжной цветовой схемы.

---

## Lesson 13: [2025-12-17] Batch режимы в CALF

**Проблема**: При использовании VectorizedEnvironment режим всегда "td3", хотя safety метрика меняется.

**Причина**: VectorizedEnvironment вызывает `get_actions_batch()`, но `current_mode` обновляется только при `get_action()` (single).

**Решение**:
```python
# ❌ НЕ РАБОТАЕТ в batch режиме:
mode = calf_policy.current_mode  # Не обновляется!

# ✅ РАБОТАЕТ:
mode = calf_policy.get_mode_for_env(i)  # Для i-го агента
```

**В CALFPolicy**:
- `get_action()` → обновляет `self.current_mode`
- `get_actions_batch()` → обновляет `self.batch_modes[]`
- Используй `get_mode_for_env(i)` для получения режима конкретного агента

**Вывод**: Batch режимы требуют отдельного хранилища (`batch_modes[]`) и getter метода.

---

## Lesson 14: [2025-12-17] Производительность с множеством Entity

**Проблема**: При 50 агентах с траекториями (400+ entities) падает FPS.

**Причина**: Каждый сегмент траектории = отдельный Entity. Много entities = медленно.

**Оптимизации**:
```python
# Для большого количества агентов:
trail = MultiColorTrail(
    max_length=800,      # Меньше точек (было 1500)
    decimation=2,        # Каждая 2-я (было 1)
    rebuild_frequency=10  # Реже (было 5)
)
```

**Результаты**:
- 10 агентов: decimation=1, rebuild=5 → отличный FPS
- 50 агентов: decimation=2, rebuild=10 → приемлемый FPS

**Вывод**: Просадка FPS с множеством entities нормальна. Адаптируй параметры под количество агентов.

---

## Lesson 15: [2025-12-17] Python shutdown warnings

**Проблема**: При закрытии приложения появляются ошибки:
```
Exception ignored in: <function MultiColorTrail.__del__>
ImportError: sys.meta_path is None, Python is likely shutting down
```

**Причина**: Деструктор `__del__()` пытается очистить визуальные объекты, но Python уже начал выгружать модули.

**Решение**:
```python
def __del__(self):
    """Очистка при удалении объекта"""
    try:
        import sys
        # Проверяем что Python еще не завершается
        if sys.meta_path is not None:
            self.clear()
    except:
        # Игнорируем любые ошибки при shutdown
        pass
```

**Вывод**: Всегда проверяй `sys.meta_path is not None` в деструкторах, которые используют сторонние модули.

---

## Lesson 16: [2025-12-17] Визуальные объекты и ZoomManager

**Проблема**: Объекты, созданные через `Entity()` напрямую (без ObjectManager), не реагируют на zoom.

**Примеры объектов, которые НЕ ДОЛЖНЫ масштабироваться**:
- UI элементы (Text, Button, etc.)
- Sky
- Directional lights

**Примеры объектов, которые ДОЛЖНЫ масштабироваться**:
- 3D визуальные объекты (сферы, кубы, траектории)
- Маркеры и boundary frames
- Critic heatmap surface

**Решение**:
```python
# ❌ НЕПРАВИЛЬНО - объект не будет масштабироваться
self.surface_entity = Entity(
    model=mesh,
    double_sided=True
)

# ✅ ПРАВИЛЬНО - создать через ObjectManager
# (или зарегистрировать после создания)
zoom_manager.register_object(self.surface_entity, 'name')
```

**Вывод**: Все 3D визуальные объекты должны управляться через ObjectManager для корректного zoom.

---

## Lesson 17: [2025-12-17] Интеграция визуальных компонентов с ZoomManager

**Проблема**: Фрейм реагирует на зум, но агенты, траектории и heatmap - нет.

**Причина**:
1. Агенты - обновляли `position` напрямую, а не `real_position`
2. Траектории и Heatmap - создавались как обычные `Entity`, не регистрировались в ZoomManager

**Решение - 3 шага**:

**1. Правильное обновление позиций агентов**:
```python
# ✅ ПРАВИЛЬНО - обновляем real_position и применяем трансформацию:
visual_points[i].real_position = np.array(new_position)
visual_points[i].apply_transform(
    zoom_manager.a_transformation,
    zoom_manager.b_translation
)
```

**2. Регистрация траекторий в ZoomManager**:
```python
trail = MultiColorTrail(...)
zoom_manager.register_object(trail, f'trail_{i}')
```

**3. Добавление методов совместимости в визуальные классы**:
```python
class MultiColorTrail:
    def apply_transform(self, a, b, **kwargs):
        """Применить трансформацию зума"""
        self.a_transformation = a
        self.b_translation = b
        self.rebuild()

    def set_zoom_manager(self, zoom_manager):
        self.zoom_manager = zoom_manager

    @property
    def enabled(self):
        return len(self.segments) > 0
```

**Вывод**: Любой визуальный компонент для zoom должен иметь: `apply_transform()`, `set_zoom_manager()`, `enabled`.

---

# Новые уроки из Stage 2

Записывай новые уроки ниже в том же формате.

---

## Lesson 18: [2026-01-21] STAGE 1 был уже реализован

**Проблема**: План оптимизации указывал что метод `select_action_batch` отсутствует в CALFController и нужно его реализовать.

**Решение**: При проверке кода обнаружено что метод уже полностью реализован в `RL/calf.py` (lines 238-337) со всеми требуемыми оптимизациями:
- Batch actor inference (1 GPU call)
- Batch critic inference (1 GPU call)
- Vectorized certificate checking
- Q-value caching in update_certificate
- Mode handling ('td3', 'relax', 'fallback')
- Bonus: `update_state` parameter для разделения training/visualization агентов

**Вывод**: Всегда проверяй текущее состояние кода перед началом работы. STAGE 1 требовал только верификации, а не реализации. План был создан до завершения Phase 9, где метод уже был добавлен.

---

## Lesson 19: [2026-01-21] Config folder already existed

**Проблема**: При создании config системы для STAGE 2 обнаружил что папка `ursina/config/` уже существовала с файлами `ui_constants.py` и `colors.json`.

**Решение**:
- Использовал существующую структуру
- Обновил `__init__.py` для добавления новых exports (TrainingConfig, VisualizationConfig, AppConfig)
- Сохранил существующие файлы без изменений
- Добавил новые файлы: `training_config.py`, `visualization_config.py`, `app_config.py`

**Вывод**: Всегда проверяй существующую структуру проекта перед созданием новых папок. Интеграция с существующим кодом лучше чем дублирование.

---

## Lesson 20: [2026-01-21] Dataclass преимущества для конфигурации

**Проблема**: Нужна централизованная система конфигурации с поддержкой пресетов.

**Решение**: Использовал Python `@dataclass` для конфигурационных классов:
```python
@dataclass
class TrainingConfig:
    num_episodes: int = 500
    batch_size: int = 64
    # ... other params

    @classmethod
    def from_preset(cls, preset_name: str):
        presets = {'quick': {...}, 'standard': {...}}
        return cls(**presets[preset_name])
```

**Преимущества**:
- Автоматическая генерация `__init__`, `__repr__`, `__eq__`
- Type hints для IDE autocomplete
- Default values встроены в определение
- Легко конвертируется в dict через `__dict__` или custom `to_dict()`
- Чистый и читаемый код

**Вывод**: Dataclass идеален для конфигурационных классов. Избегай словарей или классов с ручным `__init__` для конфигураций.

---

## Lesson 21: [2026-01-21] Unicode в Windows консоли

**Проблема**: `UnicodeEncodeError` при использовании emoji (🟢🟡🔴) в профайлере на Windows. Консоль Windows использует cp1252 encoding по умолчанию, который не поддерживает многие Unicode символы.

**Решение**: Заменены emoji на ASCII indicators: `[+]` (green <5ms), `[!]` (yellow 5-10ms), `[X]` (red >10ms). Работает везде, включая Windows консоль.

**Вывод**:
- Избегай emoji и Unicode символов в консольном выводе для кроссплатформенности
- ASCII всегда работает надежно
- Особенно важно для инструментов разработки (профайлеры, логи)

---

## Lesson 22: [2026-01-21] Context manager overhead

**Проблема**: Профайлер должен иметь минимальный overhead (<1ms), но нужно замерять каждый кадр (60+ FPS).

**Решение**:
- Используем `time.perf_counter()` вместо `time.time()` (выше точность)
- Context manager с `try/finally` для гарантированного измерения
- Метод `disable()` для pass-through режима (zero overhead)
- Достигнут overhead: **0.001ms per measurement** (1000x лучше цели!)

**Вывод**:
- Context managers - отличный паттерн для инструментирования кода
- `perf_counter()` оптимален для замеров производительности
- Всегда предусматривай возможность отключения профилирования
- Python overhead минимален если избегать сложной логики в измеряемом блоке

---

## Lesson 23: [2026-01-21] EMA для сглаживания метрик

**Проблема**: Мгновенные измерения времени сильно варьируются (spikes), сложно понять реальную производительность.

**Решение**: Использован EMA (Exponential Moving Average) с alpha=0.1:
```python
ema = alpha * new_value + (1 - alpha) * old_ema
```
- Сглаживает outliers
- Более чувствителен к недавним изменениям чем простое среднее
- Стабильная метрика для принятия решений

**Вывод**:
- EMA лучше простого среднего для real-time мониторинга
- Alpha=0.1 даёт хороший баланс (не слишком быстро, не слишком медленно)
- Сортировка по EMA показывает реальные bottlenecks, не случайные spikes

---

## Lesson 24: [2026-01-22] Ursina Text tag parsing конфликт

**Проблема**: При установке `profiler_text.text = profiler.get_report()` возникла ошибка:
```
TypeError: TextNode.set_text_color() argument 1 must be LVecBase4f, not str
```
Ursina Text по умолчанию парсит теги вида `<color>` в тексте. Строки `=====` и `-----` в отчёте профайлера интерпретировались как теги цвета.

**Решение**: Добавить `use_tags=False` при создании Text объекта:
```python
profiler_text = Text(
    text='',
    use_tags=False  # Disable tag parsing
)
```

**Вывод**:
- Ursina Text по умолчанию парсит HTML-подобные теги
- Для raw text (без форматирования) всегда использовать `use_tags=False`
- Особенно важно для динамического текста с неизвестным содержимым (логи, отчёты, данные пользователя)
- Альтернатива: избегать специальных символов `<>` в тексте

---

## Lesson 25: [2026-01-22] Разделение цветов для разных типов агентов

**Проблема**: Тренировочный агент и визуальные агенты использовали одинаковые цвета (голубой для td3), что затрудняло различение. Также синий цвет сливался с фиолетовым минимумом heatmap.

**Решение**: Параметризовать цвета trail через `mode_colors` в `LineTrail.__init__()`:
```python
# LineTrail теперь принимает custom colors
self.trail = LineTrail(
    max_points=200,
    mode_colors={
        'td3': Vec4(1.0, 0.9, 0.0, 1),  # Yellow for training
        'relax': Vec4(0.2, 0.8, 0.3, 1),
        'fallback': Vec4(1.0, 0.5, 0.1, 1)
    }
)

# VisualAgent теперь принимает trail_colors
agent = VisualAgent(..., trail_colors=custom_colors)
```

**Цветовая схема**:
| Агент | Точка | Trail td3 | Trail relax | Trail fallback |
|-------|-------|-----------|-------------|----------------|
| Визуальные | Голубой | Голубой | Зелёный | Оранжевый |
| Тренировочный | Жёлтый | Жёлтый | Зелёный | Оранжевый |

**Вывод**:
- Параметризация цветов через конструктор лучше чем глобальные константы
- Разные типы агентов должны визуально различаться
- Избегай цветов которые сливаются с фоном/heatmap

---

## Lesson 26: [2026-01-22] Session vs Global статистика

**Проблема**: CALF статистика (certification rate, intervention rate) показывала глобальные значения за всё время обучения, а не за текущую сессию. После загрузки checkpoint статистика не отражала текущее поведение.

**Решение**: Добавить отдельные счётчики для сессии в `CALFController`:
```python
# Global (сохраняется в checkpoint)
self.total_steps = 0
self.nominal_interventions = 0
self.relax_events = 0

# Session (сбрасывается при каждом запуске)
self.session_steps = 0
self.session_interventions = 0
self.session_relax_events = 0

def get_session_statistics(self):
    """Статистика текущей сессии"""
    total = max(1, self.session_steps)
    return {
        'intervention_rate': self.session_interventions / total,
        ...
    }
```

**Вывод**:
- Для мониторинга в реальном времени нужна session статистика
- Global статистика полезна для оценки общего прогресса обучения
- Разделение позволяет видеть и текущее поведение и историю

---

## Lesson 27: [2026-01-22] Прозрачность в Ursina нестабильна

**Проблема**: При использовании полупрозрачных цветов (alpha < 1) объекты иногда отображались прозрачными, иногда нет. Поведение было непредсказуемым.

**Решение**: Убрать прозрачность, использовать alpha = 1:
```python
# Было (нестабильно):
Vec4(0.3, 0.7, 1.0, 0.6)

# Стало (стабильно):
Vec4(0.3, 0.7, 1.0, 1)
```

**Вывод**:
- Прозрачность в Ursina/Panda3D требует специальной настройки (transparency mode, render order)
- Для простых случаев лучше избегать прозрачности
- Если нужна прозрачность - явно настраивать `entity.alpha` и transparency mode

---

