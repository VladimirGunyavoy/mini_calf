"""
UI константы для позиционирования элементов
"""

class UI_POSITIONS:
    """Стандартные позиции для UI элементов"""

    # Информация о позиции (левый верхний угол)
    POSITION_INFO = (-0.75, 0.45)

    # Инструкции (правый верхний угол)
    DEFAULT_INSTRUCTIONS = (0.5, 0.45)

    # Статус курсора (центр сверху)
    CURSOR_STATUS = (0, 0.45)

    # Счётчик (правый нижний угол)
    DEFAULT_COUNTER = (0.85, -0.45)

    # Информационный блок (левый нижний угол)
    DEFAULT_INFO_BLOCK = (-0.85, -0.45)

    # Отладочная информация (правый центр)
    DEFAULT_DEBUG_INFO = (0.85, 0)

    # Окно управления (левый центр)
    CONTROLS_WINDOW = (-0.85, 0.2)
    
    # Информация о камере (правый нижний угол)
    CAMERA_INFO = (0.5, -0.45)
    
    # Информация о масштабе (верхний правый угол)
    SCALE_INFO = (0.57, 0.47)
